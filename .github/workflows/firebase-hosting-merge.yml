name: Firebase Hosting Production Deploy

on:
  push:
    branches:
      - main

jobs:
  production-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3️⃣ Install dependencies
      - run: npm ci

      # 4️⃣ Normalize line endings
      - run: git add . && git checkout -- .

      # 5️⃣ Auto-fix ESLint & Prettier issues
      - name: ESLint & Prettier Auto-Fix
        run: |
          npx eslint "src/**/*.{ts,tsx,js,jsx}" --fix || true
          npx prettier --write "src/**/*.{ts,tsx,js,jsx,json,css,scss,md}"

      # 6️⃣ TypeScript check
      - run: npx tsc --noEmit

      # 7️⃣ Run unit tests
      - run: npm test

      # 8️⃣ Optional security scan
      - run: npm audit --audit-level=high || echo "No critical vulnerabilities"

      # 9️⃣ Build project
      - run: npm run build

      # 🔟 Deploy to Firebase live channel
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_9B7FB }}
          channelId: live
          projectId: portfolio-9b7fb
