name: DevOps CI/CD Pipeline - Firebase

on:
  push:
    branches:
      - main  # Production branch

jobs:
  ci-cd:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v4

      # 2️⃣ Set up Node.js
      - uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3️⃣ Install dependencies
      - run: npm ci

      # 4️⃣ Normalize line endings (avoid CRLF vs LF issues)
      - name: Normalize line endings
        run: git add . && git checkout -- .

      # 5️⃣ Lint & code quality check
      - name: ESLint & Prettier
        run: |
          npx eslint "src/**/*.{ts,tsx,js,jsx}" --max-warnings=0
          npx prettier --check "src/**/*.{ts,tsx,js,jsx,json,css,scss,md}"

      # 6️⃣ TypeScript type-check
      - name: TypeScript Check
        run: npx tsc --noEmit

      # 7️⃣ Run unit tests
      - name: Unit Tests
        run: npm test

      # 8️⃣ Optional security scan
      - name: Security Scan
        run: npm audit --audit-level=high || echo "No critical vulnerabilities"

      # 9️⃣ Build project
      - name: Build Project
        run: npm run build

      # 🔟 Deploy to Firebase Hosting
      - name: Deploy to Firebase
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: |
          npm install -g firebase-tools
          firebase deploy --only hosting --token "$FIREBASE_TOKEN"
